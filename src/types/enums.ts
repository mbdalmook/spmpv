export enum DepartmentStatus {
  Managed = "Managed",
  Acting = "Acting",
  Unmanaged = "Unmanaged",
}

export enum EmailFormat {
  FirstnameL = "firstname.L",
  FLastname = "F.lastname",
}

export enum FunctionType {
  Internal = "Internal",
  External = "External",
}

export enum UserRole {
  Staff = "Staff",
  Admin = "Admin",
  SuperAdmin = "Super Admin",
}

export enum WorkflowStatus {
  Draft = "Draft",
  Active = "Active",
}

export enum CompanyNumberAssignToType {
  Staff = "Staff",
  Function = "Function",
  Department = "Department",
}
